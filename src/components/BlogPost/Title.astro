---
import type { Post } from '../../content/types';

interface Props {
	text: Post['data']['title'];
}

const { text } = Astro.props;
---

<h1 set:html={text} />

<style>
	h1 {
		font-size: utopia.clamp(35.2, 48);
		line-height: 1.3;
		margin-bottom: 2.5rem;
	}
</style>
